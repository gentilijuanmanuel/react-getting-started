import React, { useState } from 'react';

const persons = (props) => {
    const [ personsState, setPersonsState ] = useState({
        
    }); 

    return(
        <h1>Hello!</h1>
    );
}

export default persons;
